

# def augur_filter_params(group_by = "country year", sequences_per_group="1000", min_date = "2000-01-01", min_length = "5000"):
#   return " --group-by $group_by --sequences-per-group $sequences_per_group --min-date $min_date --min-length $min_length"
# 
# def nextclade_align_params(max_indel = "100", seed_spacing = "100"):
#   return " --max-indel $max_indel --seed-spacing $seed_spacing "
# 
# def dictionary_to_params(params_dict={}):
#   params=" "
#   for key in params_dict:
#     params = params . "--$key ${params_dict[$key} "
#   return params
    

